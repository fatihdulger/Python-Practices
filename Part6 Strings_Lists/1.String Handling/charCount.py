searchStr = "Python programming is cohort ten everybody test"
findChar = input("Enter character to search for: ")
counter = 0

for aChar in searchStr:
    # aChar = P, y , t,...
    #print(aChar)
    if aChar == findChar:
        "count = 0 + 1, count = 1 + 1....."
        count = count + 1
print(f"Found a {findChar} {counter} times")


def userSearch():
    for aChar in searchStr:
    # aChar = P, y , t,...
    #print(aChar)
    if aChar == findChar:
        "count = 0 + 1, count = 1 + 1....."
        count = count + 1
print(f"Found a {findChar} {counter} times")


#### we need to fix this - fix this in your own time.